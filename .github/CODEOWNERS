# By default, the repository owner reviews all changes.
# The following two paths are intentionally unowned: they are the explicit
# whitelist for the collaborator and therefore do not require owner approval.

* @tbracquart

/hosts/V145-15AST/
/users/quentin/
