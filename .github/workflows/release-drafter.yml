name: Release Drafter

on:
  push:
    branches:
      - main

concurrency:
  group: release-drafter-${{ github.ref }}
  cancel-in-progress: false

permissions:
  contents: read

jobs:
  update_release_draft:
    permissions:
      contents: write
      pull-requests: write
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: 0

      # Calcule la version CalVer du jour : YY.MM.DD, avec un compteur
      # séparé par "-" qui s'incrémente s'il y a déjà un tag pour aujourd'hui.
      # 1er merge du jour -> 26.08.21
      # 2e merge du jour  -> 26.08.21-2
      - name: Compute CalVer
        id: calver
        run: |
          set -euo pipefail
          BASE=$(date +'%y.%m.%d')
          COUNT=$(git tag -l "v${BASE}" "v${BASE}-*" | wc -l)
          if [ "$COUNT" -eq 0 ]; then
            VERSION="$BASE"
          else
            VERSION="${BASE}-$((COUNT + 1))"
          fi
          echo "Computed version: $VERSION"
          echo "version=$VERSION" >> "$GITHUB_OUTPUT"

      - uses: release-drafter/release-drafter@34d80673e067bdc0c24568d3af899c216adcfaa9 # v7.7.0
        with:
          tag: v${{ steps.calver.outputs.version }}
          name: v${{ steps.calver.outputs.version }}
          publish: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
